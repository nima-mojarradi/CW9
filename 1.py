import pickle
class Person():
    def __init__(self,
                name,
                age,
                address)
            self.name = name
            self.age = age
            self.address = address

def pickling(obj_list):
    picle_list = open('list.pickle', "wb")
    pick.dump
